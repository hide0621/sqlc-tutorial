version: "2"
sql:
  - engine: "sqlite"
    queries: "infrastructure/sqlite/db/query"
    schema: "infrastructure/sqlite/db/schema/schema.sql"
    gen:
      go:
        package: "dbgen"
        out: "infrastructure/sqlite/db/dbgen"